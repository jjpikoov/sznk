CREATE SCHEMA sznk;

CREATE TABLE IF NOT EXISTS sznk.lecture (
    id  SERIAL PRIMARY KEY,
    title   VARCHAR(100)    NOT NULL,
    place   VARCHAR(100)    NOT NULL,
    description VARCHAR(1000)   NOT NULL
);
